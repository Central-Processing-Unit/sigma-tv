app:
    datasource:
      jdbc-url: jdbc:postgresql://localhost:5432/sigma_tv
      username: ${POSTGRES_USERNAME} # PostgreSQL username
      password: ${POSTGRES_PASSWORD} # PostgreSQL password
      pool-size: 30
spring:
    flyway:
        baseline-on-migrate: true
    application:
        name: ChesSRS Development Server
